(function ()
{
    'use strict';

    angular
        .module('fastTrackApp.connectionObject', [])
        .config(config);

    /** @ngInject */
    function config()
    {
    	
    }
})();